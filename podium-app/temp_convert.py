import sys
import re
from math import cos, sin, pi

# A very basic, rough converter for OKLCH to RGB just to get the hex colors
# Alternatively, I can just use npm to install a small conversion script.
